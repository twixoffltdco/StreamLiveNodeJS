<!DOCTYPE html>
<html lang="ru">
<%- include('../partials/head') %>
<body>
<%- include('../partials/nav') %>
<%- include('../partials/flash') %>
<div class="container">
  <div class="form-card">
    <h2>Вход в аккаунт</h2>
    <form method="POST" action="/auth/login">
      <label>Email</label>
      <input type="email" name="email" required>
      <label>Пароль</label>
      <input type="password" name="password" required>
      <button class="btn btn-primary" style="margin-top:20px;width:100%" type="submit">Войти</button>
    </form>
    <% if (providers.length) { %>
      <div class="oauth-row">
        <% providers.forEach(p => { %>
          <a class="oauth-btn" href="/auth/<%= p.name %>">
            <% if (p.icon_url) { %><img src="<%= p.icon_url %>" alt=""><% } %>
            Войти через <%= p.display_name %>
          </a>
        <% }) %>
      </div>
    <% } %>
    <p style="margin-top:18px;font-size:13px;color:var(--text-dim)">Нет аккаунта? <a href="/auth/register" style="color:var(--accent-2)">Зарегистрироваться</a></p>
  </div>
</div>
</body>
</html>
